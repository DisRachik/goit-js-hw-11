const formEl = document.querySelector('.form');
const gallery = document.querySelector('.gallery');
const inputEl = document.querySelector('.form [name="searchQuery"]');

export { formEl, gallery, inputEl };
